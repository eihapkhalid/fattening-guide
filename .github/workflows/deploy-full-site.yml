name: Deploy full fattening site

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy-full-site:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment temporarily paused while lecture 5 payload is uploaded."
