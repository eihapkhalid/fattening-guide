name: Deploy full fattening site

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy-full-site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Manual hold
        run: echo "Upload in progress"
